# Agent Instructions

- Product name is **Blind Karaoke**.
- Never display or use `wfwebapp` as product/app branding; it is only an external workflow name.
- Treat `SPEC.md` and `docs/DELIVERY_1.md` as authoritative for current implementation scope.
- Update `SPEC.md` before code changes that alter requirements.
- Update `ROADMAP.md` whenever features are added/completed.
- Do not implement future-delivery features while working on Delivery 1.
- Do not fake matching, realtime synchronization, confirmation states, or buttons.
- Prefer small cohesive modules and preserve existing working functionality.
- After implementation, run/perform all realistically available tests and record limitations.
